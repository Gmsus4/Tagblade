---
import { partners } from "../../js/partners";
import Popover from "./Popover.astro";
---

<div class="items-center justify-center h-full grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 w-full gap-6">
  {partners.map(user => (
    <Popover {...user} />
  ))}
</div>