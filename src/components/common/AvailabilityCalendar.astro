  <!-- Calendario de disponibilidad -->
  <div
    class="bg-gray-50 dark:bg-gray-800 rounded-lg p-6 shadow-md border border-gray-100 dark:border-gray-700"
    data-aos="fade-up"
    data-aos-duration="800"
    data-aos-delay="400"
  >
    <h4 class="font-medium text-gray-900 dark:text-white mb-4">
      Disponibilidad para reuniones
    </h4>
    <div class="grid grid-cols-7 gap-1 text-center mb-2">
      <div class="text-xs text-gray-500 dark:text-gray-400 font-medium">
        Lun
      </div>
      <div class="text-xs text-gray-500 dark:text-gray-400 font-medium">
        Mar
      </div>
      <div class="text-xs text-gray-500 dark:text-gray-400 font-medium">
        Mié
      </div>
      <div class="text-xs text-gray-500 dark:text-gray-400 font-medium">
        Jue
      </div>
      <div class="text-xs text-gray-500 dark:text-gray-400 font-medium">
        Vie
      </div>
      <div class="text-xs text-gray-500 dark:text-gray-400 font-medium">
        Sáb
      </div>
      <div class="text-xs text-gray-500 dark:text-gray-400 font-medium">
        Dom
      </div>
    </div>
    <div class="grid grid-cols-7 gap-1">
      {
        Array(7)
          .fill(0)
          .map((_, i) => (
            <div
              class={`p-2 rounded-md text-center ${i < 6 ? "bg-green-100 dark:bg-green-900 text-green-800 dark:text-green-200" : "bg-red-100 dark:bg-red-900 text-red-800 dark:text-red-200"}`}
            >
              <span class="text-xs">{i < 6 ? "9-18h" : "Cerrado"}</span>
            </div>
          ))
      }
    </div>
    <p class="text-sm text-gray-500 dark:text-gray-400 mt-3 text-center">
      Horario de atención para reuniones y consultas
    </p>
  </div>