---
import { Image } from "astro:assets";
import Button from "../common/Button.astro";
import { contactInfo } from "../../js/contactInfo";
import BtnScroll from "../common/BtnScroll.astro";
import Down from "../icons/Down.astro";
import Whatsapp from "../icons/Whatsapp.astro";
import Check from "../icons/Check.astro";
import Code from "../icons/Code.astro";
---

<section id="home" class="relative md:px-20 sm:px-10 px-5 bg-background dark:bg-dark-background bg-blend-multiply py-20 h-dvh flex items-center overflow-hidden" aria-label="Sección principal: inicio">
  <!-- Contenido principal del hero -->
  <div class="w-full max-w-screen-xl mx-auto grid lg:grid-cols-2 gap-12 items-center z-20">
    <!-- Columna izquierda: Texto y CTA -->
    <div class="flex flex-col items-center lg:items-start text-center lg:text-left">
      <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 dark:bg-primary/20 text-primary dark:text-primary-hover text-sm font-medium mb-6">
        <Check class="w-5 h-5"/>
        Diseño web profesional
      </div>
      
      <h1 class="mb-4 text-4xl font-extrabold tracking-tight leading-none text-text-dark dark:text-text-light md:text-5xl lg:text-6xl">
        Tu sitio web, <span class="text-primary">construido para durar</span>.
      </h1>
      
      <p class="mb-8 text-lg font-normal text-gray-700 text-text-secundary1 dark:text-subtitle-light max-w-2xl">
        Diseñamos y desarrollamos sitios web que combinan rendimiento, estética y propósito para impulsar tu negocio al siguiente nivel.
      </p>
      
      <div class="flex flex-col space-y-4 sm:flex-row sm:space-y-0 sm:space-x-4">
        <Button className="bg-primary dark:bg-primary-bright dark:text-dark-background text-text-light font-bold" title="Ver nuestros planes" url="/planes" icon={Code} iconSize="6" iconColor="dark:text-text-dark text-text-light "/>
        <Button className="bg-primary dark:bg-primary-bright dark:text-dark-background text-text-light font-bold" title="Envíanos un WhatsApp" url={`https://wa.me/52${contactInfo.phone}`} icon={ Whatsapp } iconSize="6" iconColor="dark:text-text-dark text-text-light "/>
      </div>
    </div>  
    
    <!-- Columna derecha: Ilustración o mockup -->
    <div class="hidden lg:flex justify-center items-center relative">
      <!-- Mockup de dispositivos -->
      <div class="relative w-full max-w-md">
        <!-- Laptop mockup -->
        <div class="relative z-20 transform">
          <div class="bg-transparent rounded-t-xl">
            <div class="rounded-lg overflow-hidden">
              <Image loading={"eager"} src="/mockupLaptop.webp" alt="Vista previa de un sitio web en Laptop" class="w-full h-auto" width={400} height={800}/>
            </div>
          </div>
        </div>
        
        <!-- Tablet mockup -->
        <div class="absolute -bottom-10 -right-10 z-10 w-48 transform">
          <div class="bg-transparent rounded-xl">
            <div class="rounded-lg overflow-hidden">
              <Image loading={"eager"} src="/mockupTablet.webp" alt="Vista previa de un sitio web en Tablet" class="w-full h-auto" width={400} height={800}/>
            </div>
          </div>
        </div>
        
        <!-- Phone mockup -->
        <div class="absolute -bottom-10 -left-5 z-30 w-32 transform">
          <div class="bg-transparent rounded-xl">
            <div class="rounded-lg overflow-hidden">
              <Image  loading={"eager"} src="/mockupMobile.webp" alt="Vista previa de un sitio web en móvil" class="w-full h-auto" width={400} height={800}/>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Scroll down indicator -->
  <div class="absolute bottom-8 z-30 left-0 right-0 flex items-center justify-center">
    <BtnScroll title="Más información" url="#servicios">
      <Down class="w-5 h-5"/>
    </BtnScroll>
  </div>
</section>